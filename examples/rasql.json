{
  "dialect": "sqlite",
  "migrations": "db/migrations",
  "package": "store",
  "output": "store",
  "emitter": "compact",
  "queries": [
    {
      "id": "user_by_email",
      "input": "queries/user_by_email.sql",
      "engine": "sqlite",
      "function": "UserByEmail",
      "output": "user_by_email_gen.go",
      "operation": "select",
      "cardinality": "maybe",
      "parameters": [{"name": "email", "scalar": "text", "nullable": false}],
      "results": [
        {"name": "id", "scalar": "integer", "nullable": false},
        {"name": "email", "scalar": "text", "nullable": false}
      ]
    },
    {
      "id": "user_by_id",
      "input": "queries/user_by_id.sql",
      "engine": "sqlite",
      "function": "UserByID",
      "output": "user_by_id_gen.go",
      "operation": "select",
      "cardinality": "one",
      "parameters": [{"name": "id", "scalar": "integer", "nullable": false}],
      "results": [
        {"name": "id", "scalar": "integer", "nullable": false},
        {"name": "email", "scalar": "text", "nullable": false}
      ]
    }
  ]
}
